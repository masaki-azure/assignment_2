# PULL_REQUEST.md

## What：何を変えたか
セキュリティリスクの排除、可用性向上、およびコード品質担保のため、以下の修正を行いました。

1. **ネットワークの閉域化**
   - Storage Account：パブリックアクセスを無効化し、Private Endpoint経由での接続に変更。
   - PostgreSQL：パブリックアクセスを無効化し、VNet統合（インジェクション）を適用。Private DNS Zoneを追加。
   - PostgreSQLの全開放FWルール（`0.0.0.0/0`）を削除。
2. **App Serviceの可用性改善**
   - コールドスタート防止のため、`always_on = true` に変更（合わせて `https_only = true` も設定）。
3. **シークレットのハードコード排除**
   - `variables.tf` のデフォルト値からDB管理者ユーザー・パスワードを削除。
   - 該当変数に `sensitive = true` を付与し、外部（TF_VAR等）からの注入を前提とする作りに変更。
4. **CIパイプライン（GitHub Actions）の強化**
   - `ci.yml` に Quality Gate を実装。従来の `fmt` に加え、`validate`、`tflint`（Lintチェック）、`tfsec`（セキュリティスキャン）の実行ステップを追加。
   - `versions.tf` にてプロバイダーのバージョンを固定し、将来の破壊的変更による事故を防止

## Why：なぜ必要か（事故防止／運用／保守の観点）
- **事故防止（セキュリティ）**: 
  現状、DBとStorageがインターネットへフルオープンになっており、不正アクセスやデータ漏洩のリスクが極めて高いため。
- **運用（可用性）**: 
  `always_on = false` のままだとアイドル状態からの復帰時に大幅なレイテンシ（コールドスタート）が発生し、ユーザー体験を損なうため。
- **保守（シークレット管理と品質担保）**: 
  クレデンシャル情報がGit履歴に残る危険な状態を解消するため。また、設定ミスや脆弱性の混入を「属人的なコードレビュー」だけでなく「システム」で確実に防ぐ仕組みが必要なため。

## How：どう直したか（方針・ルール）
- **Secure by defaultの徹底**: 
  リソースのパブリックアクセスは明示的に `false` とし、VNet・Private Endpointを用いて閉域網内で完結するネットワーク設計に変更。
- **認証情報の分離**: 
  Terraformのコード内にはパスワード等のシークレットを一切持たせず、実行環境から渡す構造とした。
- **CIによる自動検証**: 
  `ci.yml` にコード静的解析（tflint）とインフラ脆弱性スキャン（tfsec）を組み込み、PR作成・更新時に自動検証する仕組みを構築。

## 再発防止：チームで守るルール、チェックの仕組み（CI等）
今回実装したCI（Quality Gate）をベースに、以下の運用ルールを徹底する。

1. **CIのPassをマージ必須条件に設定**
   - GitHubのBranch Protection Ruleにて、`quality-gate` ジョブ（tfsec, tflint等）の成功をマージの必須要件とし、脆弱性を含むコードの混入を自動でブロックする。
2. **Secret Scannerの有効化**
   - 意図せぬクレデンシャル混入に備え、GitGuardianやGitHub Advanced Security等のシークレットスキャンをリポジトリレベルで有効化する。
3. **レビュー要件の厳格化**
   - CIで検知できないアーキテクチャの妥当性やIAM権限の変更については、セキュリティ知見のあるメンバー1名以上のApproveを必須とする。